<div class="row">
  <div class="col-auto">
    <h2>Welcome, {{name}}!</h2>
  </div>
</div>

<div class="row mt-4">
  <div class="col-md-6">
    <h3>Create a New Idear:</h3>

    <form class="form new-idea-form" id="new-idea-form">
      <div class="field">
        <label for="idea-desc">idear:</label>
        <textarea class="form-input" type="text" id="idea-desc" name="idea-desc"></textarea>
      </div>
      {{!-- <div class="choice-desc field">
        <textarea placeholder="Choices" type="text" class="form-input" id="choice-desc-one" name="choice-desc"></textarea>
      </div>
      <div class="choice-desc field">
        <textarea placeholder="Choices" type="text" class="form-input" id="choice-desc-two" name="choice-desc"></textarea>
      </div>
      <div class="choice-desc field">
        <textarea placeholder="Choices" type="text" id="choice-desc-three" name="choice-desc"></textarea>
      </div>
      <div class="choice-desc field">
      <textarea placeholder="Choices"  type="text" id="choice-desc-four" name="choice-desc"></textarea>
      </div>
      <div class="choice-desc field">
        <textarea placeholder="Choices" type="text" id="choice-desc-five" name="choice-desc"></textarea>
      </div>
      <div class="field is-grouped">
        <button type="submit" class="btn btn-primary" id="create-idea">Create</button>
      </div> --}}
    </form>
  </div>

  {{#if ideas.length}}
  <div class="col-md-6 idea-list">
    <h3>Current Idears:</h3>

    {{#each ideas as |idea|}}
    <div class="row mb-2">
      <div class="col-md-8">
        <h4><a href="/idea/{{idea.id}}">{{idea.idea}}</a></h4>
      </div>
      <div class="col-md-4">
        <button class="btn btn-sm btn-danger" data-id="{{idea.id}}">DELETE</button>
      </div>
    </div>
    {{/each}}
  </div>
  {{/if}}
</div>

<script src="../public/js/newIdea.js"></script>
<script 
