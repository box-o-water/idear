<div class="box">
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p class="box">{{idea}}</p>
<form class="box">
  {{#each choices as |choice| }}
  {{!-- TODO perhaps these shouldn't be buttons --}}
  <button class="button">{{choice.name}}</button>

  {{/each}}
</form>
</div>
